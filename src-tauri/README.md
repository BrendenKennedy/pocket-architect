# Code Signing & Auto-Updater Setup

For complete setup instructions including key generation, code signing, CI/CD configuration, and frontend integration, see [`crypto/signing-keys/README.md`](../crypto/signing-keys/README.md).

This directory contains the Rust/Tauri backend implementation. The signing keys and certificates are managed in the `crypto/signing-keys/` directory.

